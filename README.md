The PDF files correspond to the publications that describe the methods.

Sample_code contains the files to compile in R and Python using the Eigen C++ library.

A dependency-free version can be install in R with `remotes::install_github('alenxav/PEGS/pegs')`
